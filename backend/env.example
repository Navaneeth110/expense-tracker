# Backend Environment Configuration
# DO NOT commit this file with real values to GitHub
# Set these values in Render's Environment Variables UI

# Database Configuration
# For local development (SQLite)
# DATABASE_URL=sqlite:///./expense_tracker.db

# For production (PostgreSQL on Render)
# DATABASE_URL=postgresql://username:password@host:port/database
# Get this from your Render PostgreSQL database settings

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# CORS Configuration
# Add your frontend URLs here (separate with commas)
# Example: ALLOWED_ORIGINS=http://localhost:3000,https://your-frontend-app.onrender.com
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Environment
ENVIRONMENT=development
